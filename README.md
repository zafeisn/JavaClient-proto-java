# JavaClient-proto-java
(GRPC call) If you use Java as the client, you need to compile the proto file

Then compile the proto file to generate a java file

# JavaClient
https://github.com/zafeisn/JavaClient
